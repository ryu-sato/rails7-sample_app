<% content_for :title, "Editing config" %>

<h1>Editing config</h1>

<%= render "form", config: @config %>

<br>

<div>
  <%= link_to "Show this config", @config %> |
  <%= link_to "Back to configs", configs_path %>
</div>
