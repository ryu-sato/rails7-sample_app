<%= form_with(model: config) do |form| %>
  <% if config.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(config.errors.count, "error") %> prohibited this config from being saved:</h2>

      <ul>
        <% config.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :parameter, style: "display: block" %>
    <%= form.text_field :parameter %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
